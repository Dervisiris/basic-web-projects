# 📸 Modern Animated Photo Gallery

A stylish and animated photo gallery project. It is developed using **only HTML5, CSS3, and Vanilla JavaScript.** This project utilizes **no external frameworks or libraries**. It is lightweight, fast, and relies entirely on modern web standards.

## ✨ Features

* **Zero Dependencies:** Built with 100% pure code. No frameworks needed.
* **Automatic Slideshow:** The slides change automatically after a specific time.
* **Visual Progress Bar:** An animated bar that shows when the next picture will appear.
* **Glassmorphism Effect:** A modern "frosted glass" look on the text boxes.
* **Zoom-in Animation:** Images grow slightly when they appear for a cinematic effect.
* **Full Control:** Manual control with buttons and navigation dots.
* **Synchronization:** The timer resets automatically when you change a slide manually.

## 🛠️ Technologies Used

* **HTML5:** Semantic structure.
* **Vanilla CSS3:** Flexbox, Variables, and Animations (No CSS Frameworks).
* **Vanilla JavaScript (ES6):** DOM Manipulation and Logic (No JS Libraries).

## 📂 File Structure

```text
/ProjectFolder
  ├── index.html    # Main structure
  ├── style.css     # Styles (Pure CSS)
  └── script.js     # Logic (Pure JS)